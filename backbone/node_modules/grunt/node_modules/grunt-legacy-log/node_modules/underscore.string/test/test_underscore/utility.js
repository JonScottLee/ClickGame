$(document).ready(function() {

  var templateSettings;

  module("Utility", {

    setup: function() {
      templateSettings = _.clone(_.templateSettings);
    },

    teardown: function() {
      _.templateSettings = templateSettings;
    }

  });

  test("#750 - Return _ instance.", 2, function() {
    var instance = _([]);
    ok(_(instance) === instance);
    ok(new _(instance) === instance);
  });

  test("identity", function() {
    var moe = {name : 'moe'};
    equal(_.identity(moe), moe, 'moe is the same as his identity');
  });

  test("uniqueId", function() {
    var ids = [], i = 0;
    while(i++ < 100) ids.push(_.uniqueId());
    equal(_.uniq(ids).length, ids.length, 'can generate a globally-unique stream of ids');
  });

  test("times", function() {
    var vals = [];
    _.times(3, function (i) { vals.push(i); });
    ok(_.isEqual(vals, [0,1,2]), "is 0 indexed");
    //
    vals = [];
    _(3).times(function (i) { vals.push(i); });
    ok(_.isEqual(vals, [0,1,2]), "works as a wrapper");
  });

  test("mixin", function() {
    _.mixin({
      myReverse: function(string) {
        return string.split('').reverse().join('');
      }
    });
    equal(_.myReverse('panacea'), 'aecanap', 'mixed in a function to _');
    equal(_('champ').myReverse(), 'pmahc', 'mixed in a function to the OOP wrapper');
  });

  test("_.escape", function() {
    equal(_.escape("Curly & Moe"), "Curly &amp; Moe");
    equal(_.escape("Curly &amp; Moe"), "Curly &amp;amp; Moe");
    equal(_.escape(null), '');
  });

  test("_.unescape", function() {
    var string = "Curly & Moe";
    equal(_.unescape("Curly &amp; Moe"), string);
    equal(_.unescape("Curly &amp;amp; Moe"), "Curly &amp; Moe");
    equal(_.unescape(null), '');
    equal(_.unescape(_.escape(string)), string);
  });

  test("template", function() {
    var basicTemplate = _.template("<%= thing %> is gettin' on my noives!");
    var result = basicTemplate({thing : 'This'});
    equal(result, "This is gettin' on my noives!", 'can do basic attribute interpolation');

    var sansSemicolonTemplate = _.template("A <% this %> B");
    equal(sansSemicolonTemplate(), "A  B");

    var backslashTemplate = _.template("<%= thing %> is \\ridanculous");
    equal(backslashTemplate({thing: 'This'}), "This is \\ridanculous");

    var escapeTemplate = _.template('<%= a ? "checked=\\"checked\\"" : "" %>');
    equal(escapeTemplate({a: true}), 'checked="checked"', 'can handle slash escapes in interpolations.');

    var fancyTemplate = _.template("<ul><% \
      for (key in people) { \
    %><li><%= people[key] %></li><% } %></ul>");
    result = fancyTemplate({people : {moe : "Moe", larry : "Larry", curly : "Curly"}});
    equal(result, "<ul><li>Moe</li><li>Larry</li><li>Curly</li></ul>", 'can run arbitrary javascript in templates');

    var escapedCharsInJavascriptTemplate = _.template("<ul><% _.each(numbers.split('\\n'), function(item) { %><li><%= item %></li><% }) %></ul>");
    result = escapedCharsInJavascriptTemplate({numbers: "one\ntwo\nthree\nfour"});
    equal(result, "<ul><li>one</li><li>two</li><li>three</li><li>four</li></ul>", 'Can use escaped characters (e.g. \\n) in Javascript');

    var namespaceCollisionTemplate = _.template("<%= pageCount %> <%= thumbnails[pageCount] %> <% _.each(thumbnails, function(p) { %><div class=\"thumbnail\" rel=\"<%= p %>\"></div><% }); %>");
    result = namespaceCollisionTemplate({
      pageCount: 3,
      thumbnails: {
        1: "p1-thumbnail.gif",
        2: "p2-thumbnail.gif",
        3: "p3-thumbnail.gif"
      }
    });
    equal(result, "3 p3-thumbnail.gif <div class=\"thumbnail\" rel=\"p1-thumbnail.gif\"></div><div class=\"thumbnail\" rel=\"p2-thumbnail.gif\"></div><div class=\"thumbnail\" rel=\"p3-thumbnail.gif\"></div>");

    var noInterpolateTemplate = _.template("<div><p>Just some text. Hey, I know this is silly but it aids consistency.</p></div>");
    result = noInterpolateTemplate();
    equal(result, "<div><p>Just some text. Hey, I know this is silly but it aids consistency.</p></div>");

    var quoteTemplate = _.template("It's its, not it's");
    equal(quoteTemplate({}), "It's its, not it's");

    var quoteInStatementAndBody = _.template("<%\
      if(foo == 'bar'){ \
    %>Statement quotes and 'quotes'.<% } %>");
    equal(quoteInStatementAndBody({foo: "bar"}), "Statement quotes and 'quotes'.");

    var withNewlinesAndTabs = _.template('This\n\t\tis: <%= x %>.\n\tok.\nend.');
    equal(withNewlinesAndTabs({x: 'that'}), 'This\n\t\tis: that.\n\tok.\nend.');

    var template = _.template("<i><%- value %></i>");
    var result = template({value: "<script>"});
    equal(result, '<i>&lt;script&gt;</i>');

    var stooge = {
      name: "Moe",
      template: _.template("I'm <%= this.name %>")
    };
    equal(stooge.template(), "I'm Moe");

    if (!$.browser.msie) {
      var fromHTML = _.template($('#template').html());
      equal(fromHTML({data : 12345}).replace(/\s/g, ''), '<li>24690</li>');
    }

    _.templateSettings = {
      evaluate    : /\{\{([\s\S]+?)\}\}/g,
      interpolate : /\{\{=([\s\S]+?)\}\}/g
    };

    var custom = _.template("<ul>{{ for (key in people) { }}<li>{{= people[key] }}</li>{{ } }}</ul>");
    result = custom({people : {moe : "Moe", larry : "Larry", curly : "Curly"}});
    equal(result, "<ul><li>Moe</li><li>Larry</li><li>Curly</li></ul>", 'can run arbitrary javascript in templates');

    var customQuote = _.template("It's its, not it's");
    equal(customQuote({}), "It's its, not it's");

    var quoteInStatementAndBody = _.template("{{ if(foo == 'bar'){ }}Statement quotes and 'quotes'.{{ } }}");
    equal(quoteInStatementAndBody({foo: "bar"}), "Statement quotes and 'quotes'.");

    _.templateSettings = {
      evaluate    : /<\?([\s\S]+?)\?>/g,
      interpolate : /<\?=([\s\S]+?)\?>/g
    };

    var customWithSpecialChars = _.template("<ul><? for (key in people) { ?><li><?= people[key] ?></li><? } ?></ul>");
    result = customWithSpecialChars({people : {moe : "Moe", larry : "Larry", curly : "Curly"}});
    equal(result, "<ul><li>Moe</li><li>Larry</li><li>Curly</li></ul>", 'can run arbitrary javascript in templates');

    var customWithSpecialCharsQuote = _.template("It's its, not it's");
    equal(customWithSpecialCharsQuote({}), "It's its, not it's");

    var quoteInStatementAndBody = _.template("<? if(foo == 'bar'){ ?>Statement quotes and 'quotes'.<? } ?>");
    equal(quoteInStatementAndBody({foo: "bar"}), "Statement quotes and 'quotes'.");

    _.templateSettings = {
      interpolate : /\{\{(.+?)\}\}/g
    };

    var mustache = _.template("Hello {{planet}}!");
    equal(mustache({planet : "World"}), "Hello World!", "can mimic mustache.js");

    var templateWithNull = _.template("a null undefined {{planet}}");
    equal(templateWithNull({planet : "world"}), "a null undefined world", "can handle missing escape and evaluate settings");
  });

  test('_.template provides the generated function source, when a SyntaxError occurs', function() {
    try {
      _.template('<b><%= if %></b>');
    } catch (e) {
      ok(e.source.indexOf('( if )') > 0);
    }
  });

  test('_.template handles \\u2028 & \\u2029', function() {
    var tmpl = _.template('<p>\u2028<%= "\\u2028\\u2029" %>\u2029</p>');
    strictEqual(tmpl(), '<p>\u2028\u2028\u2029\u2029</p>');
  });

  test('result calls functions and returns primitives', function() {
    var obj = {w: '', x: 'x', y: function(){ return this.x; }};
    strictEqual(_.result(obj, 'w'), '');
    strictEqual(_.result(obj, 'x'), 'x');
    strictEqual(_.result(obj, 'y'), 'x');
    strictEqual(_.result(obj, 'z'), undefined);
    strictEqual(_.result(null, 'x'), null);
  });

  test('_.templateSettings.variable', function() {
    var s = '<%=data.x%>';
    var data = {x: 'x'};
    strictEqual(_.template(s, data, {variable: 'data'}), 'x');
    _.templateSettings.variable = 'data';
    strictEqual(_.template(s)(data), 'x');
  });

  test('#547 - _.templateSettings is unchanged by custom settings.', function() {
    ok(!_.templateSettings.variable);
    _.template('', {}, {variable: 'x'});
    ok(!_.templateSettings.variable);
  });

  test('#556 - undefined template variables.', function() {
    var template = _.template('<%=x%>');
    strictEqual(template({x: null}), '');
    strictEqual(template({x: undefined}), '');

    var templateEscaped = _.template('<%-x%>');
    strictEqual(templateEscaped({x: null}), '');
    strictEqual(templateEscaped({x: undefined}), '');

    var templateWithProperty = _.template('<%=x.foo%>');
    strictEqual(templateWithProperty({x: {} }), '');
    strictEqual(templateWithProperty({x: {} }), '');

    var templateWithPropertyEscaped = _.template('<%-x.foo%>');
    strictEqual(templateWithPropertyEscaped({x: {} }), '');
    strictEqual(templateWithPropertyEscaped({x: {} }), '');
  });

  test('interpolate evaluates code only once.', 2, function() {
    var count = 0;
    var template = _.template('<%= f() %>');
    template({f: function(){ ok(!(count++)); }});

    var countEscaped = 0;
    var templateEscaped = _.template('<%- f() %>');
    templateEscaped({f: function(){ ok(!(countEscaped++)); }});
  });

  test('#746 - _.template settings are not modified.', 1, function() {
    var settings = {};
    _.template('', null, settings);
    deepEqual(settings, {});
  });

  test('#779 - delimeters are applied to unescaped text.', 1, function() {
    var template = _.template('<<\nx\n>>', null, {evaluate: /<<(.*?)>>/g});
    strictEqual(template(), '<<\nx\n>>');
  });

});
$„,ŸŞ´>=IIÊ s9»–!üXÒ›O°Äó_úKUÍÃíñ‰æ#õjõñõ3kùeC3‡„œpÎÜ»j#©Ş‹ì‰ŒLo1¾‡-<´i\Ãn-¸fb?%ğœäÙUn€u]-_\ñ(š„1V‡xà4W»A?ñ¢,vÜèéªÕìÜe‚b•â³„©°µl®Ö1¢îÛA~,?ÍaŠOÙõ²\¡“O›Pv¥Ù„ôB7f"J×n¢àÄô(Ìù/Á\%@½â”)á÷®2‡D5Ü5~+*¯ÁzHÃÏ¾PŒIÊş;(P‡+©Ü½H¤‰’¨4sÔ}–Åü¾Š¹¸è¿
äŠ6Wéb´¤²ô6'¢‹úXlQèn3BÀ5ŠÀF&\a‹$…NPà±.‘&møÇMItJÛ)ßF«&ÈÆµë;úÎÍe†6Ho	(ûT»±Ÿƒç) P¯i½"V–ña} É†Ä2(æï³üé4aç‚ğ?³¦0¥~àÙMN<®¢"ƒ?Rü k’½Ø	¯L<È^ƒcjÆú2+ßÒß‹»ïîquÈ³üš©‡ İ¯Ì{ËZ­Ÿ¤ë¹Õì Ê<ßmš¡İY‡u$íÃg”¾QÆ’Šäl•Õ
	hŞÍ½|Ä0¢’àuG¶MÄ #XÊäÃö$kôæ‚¾$?¿³íØ¿É@dµ•ö¹6Z%¡8V	í…ÖCÈ@êØ´ïÜÆ¯rwVtZ?¦éÖÌÏlŞãOµz¼ EXíöwÌ”\Wƒ±†zN–cîmk‰'t®‡»n
-@t´ø +Z8í`œbÌÍgp(È›âğ8eƒ¿.úáÔLSÒ²8ÏÒß%–%·n“ŸıÔ=¹6 Üë‡@¥;~|Pvæ‚¨¨íÊjèOMmC=2vkÅ#úüXÊû>%\MYÑˆ:Ÿéy<œŸúö·´$Îå&€9HB< ƒ<
ãEİc>Vª+¸ôÀ×Ûoš(÷dé%"Æ7oQ²±,¥ŠŠJÓ+cıúïEÜqøk¹“€e€VG[•ø“£x|Sê+xCuƒ™©¶Ükğ‚ßÑúîK™W|:@ºµğ(ƒ°%¬ßch‡]t‰½­%„BásIÈH–YlÊ‰sÔo8ô0È¶•áï>†õ‡àO)ñòwdµŠÉº'¾¼Á“Î¾}×Ê×Ú:Ì=[¶ç’Şô¹È¼Á:Ú ğE\^3yöUªu³š	M\&¡±¢zLE´IRbÔæÙ@hËjñXÛ°íû›ğ	¤ïYì9õdŞß^Œ¤jE•ìıñ7¡"$Èá÷@f©fÛ?GñÿÕ º'fl¤ÛòO oÃZqÿb7?ntÿ9ˆ/Œ(İµ ~g6¼Ù¡ş‹I¬ÀOH†Ÿğw¨8]È(>v8oºT²—{©ÿ¶·¢UfAÌ·;Å8)û]oó74÷—²eú9~A£Ø/Ğ‹•`Ğ÷ğÉÎıúR…ÅÍ,^l™»ÏœOqğJû¬UÓ¢	‘¬%+C'VŸ×MVRış‰£èk-ˆY¨Òø+T`JŞ\ÜY9Õ¡roávŒ›²ÀñByÇpm›×ÚÊmÏœô_‹šAi ZTIÑSŒf¸±§r#È^vFğ;áâhVÏÚÜ¡é0• ùGÕI’©gCîx©ëôÿİr¯ªT1îqsİØdbßšñZv£ı3§ÜfÅcU-í¨âõ^œ¶¹¥±CÑŠ´5²H|ñtÚ¨½#ÜÉ¿•cSè[eö<ù3Ew2íb-øË<9Ü ÷üù%ÿÓ•íÈådü~ªût>ØüÉÄ±ª¶çs@/-lL©„ğ¶ÖˆEdîªU…Yá„‚f€nDrî;"¦&[Á–İ÷¬÷Ÿ^Û¶ñ¶ÿ ªñşb9i¥ÌfÊhÍ½X^Z ñ h)»¶îçwÅJøhõƒàHÌq¯¤ÓşÜ¡ÄŒï«ÛNµ3pâ¿ş÷#.E—àÁBŠ‡ïy	72ËáƒÙŠB‘ÜtíLØP)2H¶BÓ­\ïF7Yœ!~m-GáYXúPÊ´4²Ê4úA@™»2ä¦~e]öW‘°¬İi‘ ’9ê )ôZÇÚRÙ=ÛHèd®îåVóš,T‰-8TX_ø†×‹vo»ÀşMèêãÆàDl…Ã)hgÛ<pi"j„€&Ÿpïfø›@ÀÆğ6B'n¨M“qá%bØfùë÷Íƒ 7íå@—©
H"jÙ·ï8¨3ÉšËCØù¬Œ“eİúp$äY;Ïã5¤×vœÁÅot¤Z2–Ìñô¯­ü;íÅ§ÓÒ¢õïnT)k¬%Áô’Z´ÁhÁf¨Ø—'hšÃ•rmŞö´ss©]j¬g\O!£´	³Ì”Ñõ…(jŠä"í²y§»OúŒë–B$Â¾æ”„.cßYrz‹L7{a@¬*’RANâuxˆšæu°Û'MYbn6{Ÿ«'5¸¹b uù/YøÑàhL//€µ‹¿³1®òË®Öf×î­m–ÑbxŞ#¨©Ç¨KL:—iÒê–İº¹T£Q¥Y…ù$S>#Ë1iÿizâÎ˜ÿ—"êÎ¬<=ªÀıkfí&s¿Ìg·ÙIuUôƒÜŸ4šúÔ%½Ít7ÎTVxZ´ş$Ä+µË\D×2„ÑÛ©ö>Îšc±ìÓ”ºW©9çUkÅò&¦Ó›êVc¥Æ¼™Ë€î=’ÙàÓÊW!‡t3Ø¼hˆÄO<…1²Z›7²1œ˜Ûñ¶àzŞ|ÒYv“S.HÀØò—´2Lw*×¬úLFX&ÁøY›ÆS´¯ør.·½îìôâ&0¥bR§ÅrÕ€‹³æBvï×Æ¤Ì
©2‰–°#^óB&»–Tš*+ı0Ÿ2Ù¢çÍİ1¦¼P—©r´Vä1Rr/uE`¤–WÚIàqäd3p“Ñ×ƒìç^œPºƒÌ¥áÏø·•ÌL|w…:Ï7¸ŠY ø=Øâ&mÑ±‚´àºçäö‡-N«‰±V‰ÏÜö{Œë¡D¿¼é-–V°7y¿ó+à¨À£b¹pd¹E©ZÅJF¾0%9oz`£pßøzdšöˆtaB»;ßéZùğwíW·Š%)…òÍÊA’¾Ú[;,!ŠÏ’G^¢’#EÓ,ßĞW‚ÊcO*ŸÖ™&ï¸Hã×[Õ.ÙFj‘®òÀğş’°îİ~î33zĞ˜º=‹®l°¸*ïigÛÓÄğ@È£f	ßñ\K<ïÁ¼ÀÉÛ	ÆqrT©!1~èâ;¢ÚŞ¬47¬2°J¶>ÖrAØŠoP†ìÅ¯ XQ2®ÒE½,IL3ÔÓKIbÖõöÄƒmùŸèĞE^£ÑQÚH»~0`ÿm×J4—îz»UDáj{0Ç=BjÇ†b4ˆ—ttÔ h¾